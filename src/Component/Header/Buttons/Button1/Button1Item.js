const Button1Items = (props) => {
  return (
    <div>
      <button className="dropdown-item" type="button">
        <b>{props.text}</b>
      </button>
    </div>
  );
};
export default Button1Items;
